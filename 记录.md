QQBotProject/
│
├── config.py         # ⚙️ 配置模块：存放全局常量、数据库路径等
├── data_manager.py   # 💾 数据模块：负责处理 TXT 文件的读写操作
├── bot_core.py       # 🤖 核心模块：专门处理 WebSocket 连接和群/私聊逻辑
├── gui.py            # 🖥️ 界面模块：只负责 Tkinter 界面展示和按钮事件
└── main.py           # 🚀 入口模块：程序的启动点

pip install websockets dataset pyinstaller
pyinstaller -F -w main.py -n "老登群服管理中心"

引入sqlite3，管理一个QQbot数据库引入dataset模块。
读取数据库，如果没有创立
1，读取QQgroup表，如果没有建立一个
新增总开关”开启群管理“，子开关”开启群签到“。
开启后，监控群消息。@机器人账号 /功能，如“@小助手 /功能”
机器人先判断是否为群里的管理，如果不是则发送“并非本群管理，无法试用本功能”
如果是管理，
返回以下消息到群里。
1，/签到(获得1积分)
2，/黑名单QQ(查看黑名单列表)
3，/加入黑名单QQ XXXX
4，/删除黑名单QQ XXXX
5，/积分(查看成员积分)
如果是群管理，监控到 /签到，读取QQ群数据表，如果没有该成员记录，则加入。
QQ群名，QQ群号，QQ群员昵称，QQ群成员号，签到积分，签到时间。
如果有记录，如果签到时间为同一天则，返回“@群成员 签到失败，今天已经签到过了~”
如果不是同一天，则签到积分+1，签到时间更正为现在返回“@群成员 签到成功，积分+1”
如果是群管理，监控到 /积分 读取QQ群数据表，如果没有该成员记录“@群成员 没有你的记录“
如果有返回“@群成员 你拥有积分是:xx，签到时间为：2026-04-28“
其他功能等待补充